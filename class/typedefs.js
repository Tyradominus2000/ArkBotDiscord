/**
 * @typedef {Object} RconParam Param to make connection to the rcon auth
 * @property {String} host the adresse of the host
 * @property {Number} port the port of the host
 * @property {Sting} password the password to acces the rcon
 * @property {OptionRcon} options option for the rcon auth
 */

/**
 * @typedef {Object} ClientDiscord Discord Client
 */

/**
 * @typedef {Object} OptionRcon options for the rcon object
 * @property {Boolean} tcp if the rcon should use the tcp protocol
 * @property {Boolean} challenge if the rcon should use the challenge protocol
 */